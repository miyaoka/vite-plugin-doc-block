pre-commit:
  # pnpm fix と同じ内容を staged files に対して実行する。
  # 逐次実行。oxlint --fix の書き換えを oxfmt が整形するため、この順序に依存する
  jobs:
    - run: pnpm fix:oxlint {staged_files}
      glob: "*.{ts,js}"
      stage_fixed: true

    - run: pnpm fix:oxfmt {staged_files}
      glob: "*.{ts,js}"
      stage_fixed: true
